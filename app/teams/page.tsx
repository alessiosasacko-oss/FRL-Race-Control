import AppLayout from "@/components/layout/AppLayout";
import EmptyState from "@/components/ui/EmptyState";

export default function TeamsPage() {
  return (
    <AppLayout>
      <EmptyState
        title="Teams"
        description="Die Teamverwaltung wird in einer späteren Phase umgesetzt."
      />
    </AppLayout>
  );
}
